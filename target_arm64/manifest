# SPDX-FileCopyrightText: 2023 Samuel Tyler <samuel@samuelt.me>
#
# SPDX-License-Identifier: GPL-3.0-or-later
#
# arm64 manifest: bootstrap tcc 0.9.26 only. tcc-0.9.27 is a later milestone for
# arm64, so it is intentionally omitted here (cf. the generic steps/manifest,
# which builds both). checksum-transcriber and simple-patch are prerequisites
# of tcc-0.9.26/pass1.kaem (it runs checksum-transcriber and applies the arm64
# simple-patches in-chroot), so they are built first.

build: checksum-transcriber-1.0
build: simple-patch-1.0
build: tcc-0.9.26
